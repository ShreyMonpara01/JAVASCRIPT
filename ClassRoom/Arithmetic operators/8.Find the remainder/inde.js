// Define three variables and assign numerical values
let a = 10;
let b = 5;
let c = 3;

// Add the values of the first two variables together
let sum = a + b;

// Divide the result by the value of the third variable
let divisionResult = sum / c;

// Find the remainder of the division
let remainder = sum % c;

// Display the remainder
console.log("Remainder:", remainder);