const sum = () => {
    // array
  let arr = [1, 2, 3, 4, 5];
    // length 5
  let num = arr.length;
    
  let sum = 0;
    
  for (let i = 0; i < num ; i++) {
    sum += array[i];
  }
    
  return sum;
};

sum()
